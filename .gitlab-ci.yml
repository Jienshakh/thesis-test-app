stages:
  - build

variables:
  DOCKER_HOST: tcp://localhost:2375
  DOCKER_TLS_CERTDIR: ""

build:
  stage: build

  image: docker:27

  services:
    - name: docker:27-dind
      command:
        - "--host=tcp://0.0.0.0:2375"
        - "--tls=false"
        - "--insecure-registry=registry.81.26.191.148.nip.io"

  tags:
    - release

  before_script:
    - until docker info; do sleep 2; done
    - echo "$CI_REGISTRY_PASSWORD" | docker login registry.81.26.191.148.nip.io -u "$CI_REGISTRY_USER" --password-stdin

  script:
    - docker build -t registry.81.26.191.148.nip.io/root/$CI_PROJECT_NAME:$CI_COMMIT_SHORT_SHA .
    - docker push registry.81.26.191.148.nip.io/root/$CI_PROJECT_NAME:$CI_COMMIT_SHORT_SHA

    - docker build -t registry.81.26.191.148.nip.io/root/$CI_PROJECT_NAME:$CI_COMMIT_SHORT_SHA .

    - docker push registry.81.26.191.148.nip.io/root/$CI_PROJECT_NAME:$CI_COMMIT_SHORT_SHA